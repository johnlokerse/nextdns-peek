# 2026-01-12

Added a dashboard tab bar to separate the overview from a new allow list placeholder.

Code updated:
```swift
TabView {
  overviewTab
    .tabItem {
      Text("Overview")
    }

  allowListTab
    .tabItem {
      Text("Allow List")
    }
}
```

```swift
private var allowListTab: some View {
  VStack(alignment: .leading, spacing: 8) {
    Text("Allow List")
      .font(.headline)
    Text("Allow list")
      .font(.caption)
      .foregroundColor(.secondary)
  }
  .frame(maxWidth: .infinity, alignment: .leading)
}
```

Code created:
None.

Code deleted:
None.

Created a full README describing the app and all available features.

Code created:
```markdown
## Features

- Menu bar app with a SwiftUI popover.
- Overview tab with a compact stats summary (requests, blocked, block rate).
- Recent logs list with time, decision, and optional client/device details.
- Privacy mode that hides domains in the logs.
- Right-click blocked logs to add the domain to the allow list.
- Allow List tab to add domains, enable/disable entries, and remove entries.
- Resolver status indicator with POP and latency when available.
- Refresh controls: manual refresh, refresh on click, and interval-based refresh.
- Time range selection for stats/logs (Last Hour, Last 24 Hours).
- Profile selection and reload from the NextDNS API.
- API token validation, secure Keychain storage, and removal.
- Error banner handling for offline, rate-limited, and unauthorized states.
- Diagnostics panel for last refresh time and last error message.
```

Code updated:
None.

Code deleted:
None.

Lightened the navy gradient by raising the base color and lowering opacity.

Code updated:
```swift
private var backgroundGradient: LinearGradient {
  LinearGradient(
    colors: [
      Color(red: 0.08, green: 0.12, blue: 0.28, opacity: 0.22),
      Color(red: 0.08, green: 0.12, blue: 0.28, opacity: 0.05)
    ],
    startPoint: .top,
    endPoint: .bottom
  )
}
```

Code created:
None.

Code deleted:
None.

Reduced the navy gradient intensity to start at 30% opacity before fading down.

Code updated:
```swift
private var backgroundGradient: LinearGradient {
  LinearGradient(
    colors: [
      Color(red: 0.03, green: 0.07, blue: 0.2, opacity: 0.3),
      Color(red: 0.03, green: 0.07, blue: 0.2, opacity: 0.08)
    ],
    startPoint: .top,
    endPoint: .bottom
  )
}
```

Code created:
None.

Code deleted:
None.

Increased the navy gradient intensity to start at 60% opacity and fade downward.

Code updated:
```swift
private var backgroundGradient: LinearGradient {
  LinearGradient(
    colors: [
      Color(red: 0.03, green: 0.07, blue: 0.2, opacity: 0.6),
      Color(red: 0.03, green: 0.07, blue: 0.2, opacity: 0.2)
    ],
    startPoint: .top,
    endPoint: .bottom
  )
}
```

Code created:
None.

Code deleted:
None.

Increased the navy gradient opacity and moved it behind the entire dashboard to ensure it shows through the tab content.

Code updated:
```swift
ZStack {
  backgroundGradient

  VStack(alignment: .leading, spacing: 12) {
    // content
  }
  .padding(12)
  .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topLeading)
}
```

```swift
private var backgroundGradient: LinearGradient {
  LinearGradient(
    colors: [
      Color(red: 0.04, green: 0.08, blue: 0.2, opacity: 0.38),
      Color(red: 0.04, green: 0.08, blue: 0.2, opacity: 0.16)
    ],
    startPoint: .top,
    endPoint: .bottom
  )
}
```

Code created:
None.

Code deleted:
None.

Added a top-to-bottom navy gradient background for the dashboard container.

Code updated:
```swift
.padding(12)
.background(backgroundGradient)
.frame(width: contentWidth, height: contentHeight)
```

```swift
private var backgroundGradient: LinearGradient {
  LinearGradient(
    colors: [
      Color(red: 0.05, green: 0.09, blue: 0.2, opacity: 0.14),
      Color(red: 0.05, green: 0.09, blue: 0.2, opacity: 0.04)
    ],
    startPoint: .top,
    endPoint: .bottom
  )
}
```

Code created:
None.

Code deleted:
None.

Added a right-click menu on blocked logs to send the domain to the allow list.

Code updated:
```swift
LogsListView(
  logs: viewModel.logs,
  privacyMode: appState.privacyMode,
  allowListViewModel: allowListViewModel
)
```

```swift
if entry.decision == .blocked {
  logRow(entry)
    .contextMenu {
      Button("Add to Allow List") {
        Task {
          _ = await allowListViewModel.addEntry(domain: entry.domain)
        }
      }
      .disabled(!canAddToAllowList(entry))
    }
}
```

```swift
func containsDomain(_ domain: String) -> Bool {
  let trimmedDomain = domain.trimmingCharacters(in: .whitespacesAndNewlines)
  guard !trimmedDomain.isEmpty else {
    return false
  }
  return entries.contains { $0.domain.caseInsensitiveCompare(trimmedDomain) == .orderedSame }
}
```

Code created:
None.

Code deleted:
None.

Switched allow list entry updates to PATCH to match NextDNS API expectations for child array updates.

Code updated:
```swift
let data = try await request(
  path: "profiles/\(profileId)/allowlist/\(escapePathComponent(entryId))",
  method: "PATCH",
  queryItems: [],
  token: token,
  body: try JSONSerialization.data(withJSONObject: payload)
)
```

Code created:
None.

Code deleted:
None.

Added allow list fetching, add/remove actions, and enable toggles wired to the NextDNS API.

Code updated:
```swift
func fetchAllowList(profileId: String, token: String) async throws -> [AllowListEntry] {
  let data = try await request(
    path: "profiles/\(profileId)/allowlist",
    queryItems: [],
    token: token
  )
  return try decodeAllowList(from: data)
}
```

```swift
private func allowListRow(_ entry: AllowListEntry) -> some View {
  HStack(spacing: 12) {
    Text(entry.domain)
      .font(.subheadline)
      .lineLimit(1)

    Spacer()

    Toggle("", isOn: Binding(
      get: { entry.isEnabled },
      set: { viewModel.setEntryEnabled(entry, isEnabled: $0) }
    ))
    .toggleStyle(.switch)
    .labelsHidden()
  }
}
```

Code created:
```swift
struct AllowListEntry: Identifiable, Equatable {
  let id: String
  var domain: String
  var isEnabled: Bool
}
```

Code deleted:
None.

Increased the menu bar popover size by 35% and aligned the dashboard width to match.

Code updated:
```swift
let popoverScale: CGFloat = 1.35
let popoverSize = NSSize(width: 380 * popoverScale, height: 520 * popoverScale)
popover.contentSize = popoverSize

DashboardView(
  viewModel: dashboardViewModel,
  allowListViewModel: allowListViewModel,
  contentWidth: popoverSize.width,
  onOpenPreferences: onOpenPreferences
)
```

```swift
.frame(width: contentWidth)
```

Code created:
None.

Code deleted:
None.

Expanded list scroll heights to better use the larger popover.

Code updated:
```swift
.frame(maxHeight: 270)
```

```swift
.frame(maxHeight: 324)
```

Code created:
None.

Code deleted:
None.

Adjusted the popover to be wider without increasing height, and let tab content stretch to fill the available space.

Code updated:
```swift
let popoverWidthScale: CGFloat = 1.35
let popoverSize = NSSize(width: 380 * popoverWidthScale, height: 520)
```

```swift
TabView { ... }
  .frame(maxWidth: .infinity, maxHeight: .infinity)
  .layoutPriority(1)
```

```swift
.frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topLeading)
```

Code created:
None.

Code deleted:
None.

Added a NextDNS ping lookup to surface the current resolver and latency next to the active profile.

Code updated:
```swift
@Published var resolverStatus: ResolverStatus?
```

```swift
HStack(alignment: .firstTextBaseline, spacing: 8) {
  Text(appState.profile?.name ?? "No profile")
    .font(.subheadline)
    .foregroundColor(.secondary)

  Spacer()

  if let resolverStatusText {
    Text(resolverStatusText)
      .font(.caption)
      .foregroundColor(.secondary)
  }
}
```

Code created:
```swift
final class NextDNSPingClient {
  func fetchCurrentResolver() async -> ResolverStatus? {
    guard let testResult = try? await fetchTestResult() else {
      return nil
    }

    if let status = testResult.status?.lowercased(), status == "unconfigured" {
      return nil
    }

    guard let pop = testResult.pop else {
      return nil
    }

    let candidates = buildHostCandidates(ipVersion: testResult.ipVersion, method: testResult.method)
    for candidate in candidates {
      guard let info = try? await fetchInfo(host: candidate.host) else {
        continue
      }

      if info.pop == pop {
        return ResolverStatus(pop: pop, ipVersion: candidate.ipVersion, latencyMs: info.latencyMs)
      }
    }

    return ResolverStatus(pop: pop, ipVersion: testResult.ipVersion, latencyMs: nil)
  }
}
```

Code deleted:
None.
