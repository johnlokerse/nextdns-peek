# 2026-01-04

- Add menu bar SwiftUI app scaffold with status bar popover, preferences window, and shared app state.
- Implement NextDNS client, models, caching, throttled refresh, polling scheduler, and error handling.
- Add Keychain token storage and settings persistence with privacy and time range options.
- Build dashboard and preferences views for stats, logs, refresh, and diagnostics.
- Add Xcode project, scheme, and Info.plist for building the macOS app bundle.
- Align app environment initialization with main-actor isolation for SwiftUI.
- Add resilient status bar icon fallback to ensure the menu bar item is visible.
- Force accessory activation policy and visibility for the status bar item.
